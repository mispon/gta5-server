using GrandTheftMultiplayer.Server.Constant;

namespace gta_mp_server.Enums {
    /// <summary>
    /// Список скинов, которые игрок может получить на время
    /// </summary>
    public enum TemporarySkin {
        Acult = PedHash.Acult02AMY,
        Amanda = PedHash.AmandaTownley,
        AntonB = PedHash.Antonb,
        BabyD = PedHash.Babyd,
        Balla = PedHash.BallaSout01GMY,
        Brad = PedHash.Brad,
        BreakDancer = PedHash.Breakdance01AMY,
        Chef = PedHash.Chef,
        Chimp = PedHash.Chimp,
        ClayPain = PedHash.Claypain,
        Dreyfuss = PedHash.Dreyfuss,
        Gay = PedHash.Gay02AMY,
        HasJew = PedHash.Hasjew01AMM,
        Hippie = PedHash.Hippy01AMY,
        Hunter = PedHash.Hunter,
        ImpoRage = PedHash.Imporage,
        Jesus = PedHash.Jesus01,
        Juggalo = PedHash.Juggalo01AMY,
        Juggernaut = -1863364300,
        Space = PedHash.Movspace01SMM,
        Mime = PedHash.MimeSMY,
        MrsPhillips = PedHash.MrsPhillips,
        Orleans = PedHash.Orleans,
        Pig = PedHash.Pig,
        Pogo = PedHash.Pogo01,
        Pug = PedHash.Pug,
        Poodle = PedHash.Poodle,
        Priest = PedHash.PriestCutscene,
        RsRanger = PedHash.RsRanger01AMO,
        Stag = PedHash.Staggrm01AMO,
        StrPerf = PedHash.Strperf01SMM,
        Swat = PedHash.Swat01SMY,
        Tramp = PedHash.Tramp01AMO,
        Zombie = PedHash.Zombie01
    }
}